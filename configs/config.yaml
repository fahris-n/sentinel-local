routes:
  - path: "/api/hello"
    backend: "http://backend-a-service:8081"
    backendPath: "/hello"
    requiresAuth: true
    allowedRoles: 
      - user
      - premium
    maxTokens: 100 
    refillRate: 10
  - path: "/api/register"
    backend: "${BACKEND_B_ADDR}"
    backendPath: "/register"
    requiresAuth: true
    allowedRoles: 
      - user
      - premium
    maxTokens: 100
    refillRate: 10

